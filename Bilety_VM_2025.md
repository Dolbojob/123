# Билеты к экзамену по дисциплине «Вычислительные методы» (2025)

*Ответы составлены по методичке Г. С. Бондаревой «Методы вычислений» (УдГУ, 2003); там, где материал в методичке дан неполно (общая классификация методов, нормы и обусловленность, метод Гаусса, локализация собственных значений, кубический сплайн, схема Кранка–Николсона), дополнено стандартной теорией ВМ.*

---

## Билет 1. Группы методов. Математическое моделирование и вычислительный эксперимент

**Группы математических методов:**

- **Аналитические (точные)** — дают решение в виде формулы (корни квадратного уравнения, интеграл по формуле Ньютона–Лейбница). *Достоинство:* точное решение в общем виде. *Недостаток:* применимы к узкому классу задач — для большинства прикладных задач решения в замкнутой форме нет.
- **Графические** — приближённое решение построением графиков (локализация корня, грубая оценка). *Достоинство:* наглядность. *Недостаток:* малая точность.
- **Численные (вычислительные)** — сводят задачу к конечной последовательности арифметических операций и дают приближённое решение с заданной точностью. *Достоинство:* универсальность, реализуемость на ЭВМ. *Недостаток:* приближённость, накопление погрешностей, необходимость оценки точности и устойчивости.

Как отмечено во введении методички, численные методы имеют огромное значение при решении прикладных задач; при большой размерности задачи требуют большого объёма вычислений, поэтому применяют параллельные вычисления и готовые пакеты (ScaLAPACK, PINEAPL, NAG, PETSc и др.).

**Математическое моделирование** — замена реального объекта/процесса его математической моделью и исследование модели вычислительными средствами.
**Вычислительный эксперимент** — исследование объекта расчётами на ЭВМ по математической модели.

**Схема (этапы) вычислительного эксперимента:**

1. Постановка задачи, построение математической модели (физические законы → уравнения).
2. Выбор/построение численного метода (дискретизация, аппроксимация).
3. Разработка алгоритма и программы.
4. Расчёт на ЭВМ (собственно вычислительный эксперимент).
5. Анализ результатов, сопоставление с экспериментом/моделью; при необходимости — уточнение модели или метода (цикл повторяется).

**Погрешности вычислительного эксперимента:**

- **Неустранимая** = погрешность математической модели + погрешность входных данных.
- **Погрешность метода** (аппроксимации, дискретизации) — заменяем точную задачу приближённой.
- **Вычислительная (округления)** — конечная разрядность ЭВМ, накапливается при операциях.

Полная погрешность — их сумма. (Лаб. 1: приближённое число `a`, предельная абсолютная Δ и относительная δ погрешности, верные и значащие цифры.)

---

## Билет 2. Устойчивые и неустойчивые задачи. Требования к методам

**Корректность задачи (по Адамару):** решение существует, единственно и непрерывно зависит от входных данных.
Задача **устойчива (хорошо обусловлена)**, если малые возмущения данных вызывают малые изменения решения; **неустойчива (плохо обусловлена)** — если малое возмущение данных даёт большое изменение решения.

**Примеры:**

- **Корни полинома** — задача неустойчива при кратных или близких корнях: малое изменение коэффициентов сильно сдвигает корни (классический пример Уилкинсона). С этим связана **схема Горнера** (лаб. 1): значение полинома `P(x)=a₀xⁿ+...+aₙ` вычисляют по рекуррентной схеме `P₀=a₀, Pₖ=P_{k-1}·x+aₖ`, при этом погрешность накапливается меньше, чем при прямом возведении в степень, — учитывается только погрешность сложения и умножения: `ΔP₀=Δa₀, ΔPₖ=ΔP_{k-1}|x|+|P_{k-1}|Δx`.
- **Численное интегрирование** — устойчивая задача: интеграл усредняет (сглаживает) погрешности входных данных.
- **Численное дифференцирование** — неустойчивая (некорректная) задача: малое возмущение функции (особенно высокочастотный шум) даёт большое изменение производной; при `h→0` погрешность округления растёт как `1/h`.

**Общие требования к вычислительным методам:**

- **Точность** — погрешность результата ≤ заданной ε.
- **Быстродействие** — минимум операций/времени.
- **Экономичность** — экономный расход памяти и ресурсов (для больших систем критично, лаб. 5, 6 → параллельные вычисления).
- **Надёжность (устойчивость)** — устойчивость к погрешностям округления, гарантированная сходимость, отсутствие катастрофического накопления ошибок.

---

## Билет 3. Итерационный процесс. Сходимость. Устойчивость методов

**Итерационный процесс** — построение последовательности приближений `x₀, x₁, x₂, ...` по рекуррентному правилу `x_{n+1}=φ(xₙ)` (или для СЛАУ `x_{n+1}=Bxₙ+d`), сходящейся к решению. Начальное приближение `x₀` задаётся, итерации продолжают до выполнения критерия остановки (`|x_{n+1}-xₙ|<ε` или `|f(x_{n+1})|<ε`).

**Сходимость метода** — стремление приближений к точному решению: `lim xₙ = x*` при `n→∞`. Скорость сходимости:

- **линейная**: `|x_{n+1}-x*| = O|xₙ-x*|` (метод простой итерации при `q<1`);
- **квадратичная**: `|x_{n+1}-x*| = O|xₙ-x*|²` (метод Ньютона).

Условие сходимости простой итерации: `|φ′(x)| ≤ q < 1` на отрезке (формула 27); для СЛАУ `||B|| = q < 1`.

**Устойчивость метода:** метод устойчив, если погрешности (округления, начальных данных) в процессе вычислений не нарастают неограниченно. Для разностных схем (лаб. 10): схема **абсолютно устойчива**, если все `|λ|<1` при любых `h,τ`; **условно устойчива** — при некотором соотношении между `h` и `τ`; **неустойчива** — хотя бы одно `|λ|>1`. Исследуют методом разделения переменных `v_{jk}=λᵏe^{ijφ}` с применением критерия Гурвица (для `λ²+aλ+b`: `|a|≤1+b`, `|b|≤1`).

---

## Билет 4. Постановка СЛАУ. Нормы векторов и матриц. Обусловленность

**Постановка:** решить систему `Ax=b`, где `A` — невырожденная матрица `n×n`, `b` — вектор правых частей, `x` — искомый вектор. Методы делятся на **прямые** (Гаусс, квадратного корня, Холецкого) и **итерационные** (простой итерации, Якоби, Зейделя).

**Нормы вектора** `x=(x₁,...,xₙ)` (аксиомы: `||x||≥0`, `=0 ⇔ x=0`; `||αx||=|α|·||x||`; `||x+y||≤||x||+||y||`):

- `||x||₁ = Σ|xᵢ|` (октаэдрическая);
- `||x||₂ = (Σxᵢ²)^{1/2}` (евклидова);
- `||x||_∞ = max|xᵢ|` (кубическая).

(Лаб. 5, вопрос для самост. работы: проверка аксиом для `||x||_p=(Σ|xᵢ|^p)^{1/p}`.)

**Нормы матрицы** `A` (подчинённая векторной): `||A|| = max_{x≠0} ||Ax||/||x||`:

- `||A||_∞ = maxᵢ Σⱼ|aᵢⱼ|` (по строкам);
- `||A||₁ = maxⱼ Σᵢ|aᵢⱼ|` (по столбцам);
- `||A||₂ = √(λ_max(AᵀA))`.

Аксиомы матричной нормы + `||AB|| ≤ ||A||·||B||`. (В лаб. 5 — вопросы о том, является ли `||A||=Σ|aᵢⱼ|` нормой и какая норма согласована с заданной векторной для вычисления `q`.)

**Обусловленность:** число обусловленности `cond(A) = ||A||·||A⁻¹|| ≥ 1`. Характеризует чувствительность решения к погрешностям: относительная погрешность решения ≤ `cond(A)·`(относительная погрешность данных). При большом `cond(A)` задача **плохо обусловлена** (неустойчива) — малые погрешности `b` или `A` дают большую погрешность `x`; `cond(A)=1` — идеально обусловленная задача.

---

## Билет 5. Метод Гаусса

Прямой метод решения `Ax=b`. Идея — последовательное исключение неизвестных и приведение матрицы к треугольному виду.

**Прямой ход.** На `k`-м шаге исключаем `xₖ` из всех уравнений ниже `k`-го. Ведущий элемент — `a_{kk}`. Для строк `i>k`: умножаем `k`-е уравнение на `mᵢₖ = aᵢₖ/aₖₖ` и вычитаем из `i`-го:

```
aᵢⱼ⁽ᵏ⁺¹⁾ = aᵢⱼ⁽ᵏ⁾ − mᵢₖ·aₖⱼ⁽ᵏ⁾,   bᵢ⁽ᵏ⁺¹⁾ = bᵢ⁽ᵏ⁾ − mᵢₖ·bₖ⁽ᵏ⁾
```

После `n−1` шагов получаем верхнетреугольную систему.

**Обратный ход.** Находим неизвестные снизу вверх:

```
xₙ = bₙ⁽ⁿ⁾ / aₙₙ⁽ⁿ⁾
xᵢ = (bᵢ − Σ_{j=i+1}ⁿ aᵢⱼ·xⱼ) / aᵢᵢ,   i = n−1, ..., 1
```

**Выбор главного элемента.** Для устойчивости на каждом шаге переставляют строки/столбцы так, чтобы `|aₖₖ|` было максимальным (избегаем деления на малое число). Число операций — порядка `(2/3)n³`. Метод универсален (любая невырожденная матрица).

---

## Билет 6. Метод Холецкого (Халецкого)

Прямой метод (лаб. 5). Представляем `A = P·C`, где `P` — нижняя треугольная, `C` — верхняя треугольная с единицами на диагонали.

**Коэффициенты разложения** (формулы 38–39):

```
pᵢ₁ = aᵢ₁;   pᵢⱼ = aᵢⱼ − Σ_{k=1}^{j−1} pᵢₖ·cₖⱼ,  i ≥ j
c₁ⱼ = a₁ⱼ/p₁₁;   cᵢⱼ = (aᵢⱼ − Σ_{k=1}^{i−1} pᵢₖ·cₖⱼ)/pᵢᵢ,  j > i;   cᵢᵢ = 1
```

Система `Ax=b ⇔ PCx=b`. Замена `Cx=y` даёт `Py=b` (нижнетреугольная).

**Прямой ход** (находим `y`, формула 40):

```
y₁ = b₁/p₁₁;   yᵢ = (bᵢ − Σ_{k=1}^{i−1} pᵢₖ·yₖ)/pᵢᵢ,  i = 2,...,n
```

**Обратный ход** (находим `x` из `Cx=y`, формула 41):

```
xₙ = yₙ;   xᵢ = yᵢ − Σ_{k=i+1}ⁿ cᵢₖ·xₖ,  i = n−1,...,1
```

Метод остаётся применимым и при чисто мнимых коэффициентах.

> Родственный — **метод квадратного корня** (для симметричной `A`): `A = TᵀT`, `T` — верхняя треугольная, коэффициенты по формулам (35); обратный ход через `Tᵀy=b`, `Tx=y` (формулы 36–37).

---

## Билет 7. Метод простой итерации решения СЛАУ

Состоит в замене `Ax=b` эквивалентной системой `x = Bx + d` и организации итераций:

```
x_{n+1} = Bxₙ + d,   n = 0,1,...
```

Начальное приближение `x₀` произвольно (обычно `x₀=d`). 

**Условие сходимости:** `||B|| = q < 1` — тогда процесс сходится к решению при любом `x₀`. Критерий остановки:

```
||x_{n+1} − xₙ|| < (1−q)/q · ε
```

**Метод Якоби** — частный случай. Пусть `A` — матрица с диагональным преобладанием `|aᵢᵢ| > Σ_{j≠i}|aᵢⱼ|`. Переписываем `x=Bx+d`, где у `B` нули на диагонали: `bᵢⱼ = −aᵢⱼ/aᵢᵢ` (`i≠j`), `dᵢ = bᵢ/aᵢᵢ`. Полагаем `x₀=d`, `x_{n+1}=Bxₙ+d`. Для матрицы с диагональным преобладанием `q = maxᵢ Σ_{j≠i}|aᵢⱼ|/|aᵢᵢ| < 1`, сходимость гарантирована.

---

## Билет 8. Метод Зейделя

Модификация метода простой итерации (лаб. 5). `A` — матрица с диагональным преобладанием, система `x = Bx + d`. **Отличие от Якоби:** при вычислении компоненты `xᵢ^{n+1}` используются уже уточнённые на текущей итерации компоненты `x₁^{n+1}, ..., x_{i−1}^{n+1}`:

```
x₁^{n+1} = b₁₁x₁ⁿ + b₁₂x₂ⁿ + ... + b₁ₖxₖⁿ
x₂^{n+1} = b₂₁x₁^{n+1} + b₂₂x₂ⁿ + ... + b₂ₖxₖⁿ
x₃^{n+1} = b₃₁x₁^{n+1} + b₃₂x₂^{n+1} + b₃₃x₃ⁿ + ...
...
```

То есть `xᵢ^{n+1} = Σ_{j<i} bᵢⱼxⱼ^{n+1} + Σ_{j≥i} bᵢⱼxⱼⁿ + dᵢ`.

Процесс сходится несколько быстрее метода Якоби (для матриц с диагональным преобладанием всегда сходится). Условие окончания то же: `||x_{n+1} − xₙ|| = maxᵢ|xᵢ^{n+1} − xᵢⁿ| < ε`.

---

## Билет 9. Проблема собственных значений. Преобразование подобия, локализация

**Постановка:** найти собственные значения `λ` и собственные векторы `y` матрицы `A` (`n×n`): `Ay = λy`, `y≠0`. Значения `λ` — корни характеристического уравнения `P(λ) = det(A−λE) = 0`.

- **Полная проблема** — найти все собственные значения и векторы.
- **Частичная проблема** — найти максимальное (минимальное) по модулю собственное значение и соответствующий вектор.

**Преобразование подобия.** Матрицы `A` и `B` подобны, если существует невырожденная `S`: `A = S⁻¹BS`. Подобные матрицы имеют **одинаковые собственные значения** (характеристические многочлены совпадают). Собственные векторы связаны: если `By=λy`, то `x=S⁻¹y` — собственный вектор `A`. На этом основан метод Данилевского (приведение к форме Фробениуса подобными преобразованиями).

**Локализация собственных значений (теорема Гершгорина).** Все собственные значения `A` лежат в объединении кругов Гершгорина на комплексной плоскости:

```
|λ − aᵢᵢ| ≤ Σ_{j≠i} |aᵢⱼ|,   i = 1,...,n
```

— круги с центрами в диагональных элементах и радиусами, равными суммам модулей внедиагональных элементов строки. Позволяет грубо оценить расположение спектра и выбрать начальное приближение.

---

## Билет 10. Численные методы нахождения собственных значений и векторов

**Частичная проблема** (максимальное по модулю `λ`):

- **Метод простой итерации (степенной).** `x^{i+1} = Ax^i`, `x⁰ ≠ col(0,...,0)`. Для каждого `i`: `λⱼⁱ = xⱼ^{i+1}/xⱼⁱ`. Если для всех компонент `λ₁ⁱ = ... = λₙⁱ` с заданной точностью ε — это максимальное по модулю `λ`. Собственный вектор `y = x^r/||x^r||`.
- **Метод скалярных произведений.** Строят `x^{i+1}=Ax^i`, `y^{i+1}=Aᵀy^i`. Затем `λⁱ = (x^{i+1}, yⁱ)/(xⁱ, yⁱ)`. Сходится быстрее степенного.

**Полная проблема** — три этапа: 1) нахождение коэффициентов `pᵢ` характеристического многочлена `P(λ)=λⁿ − p₁λ^{n−1} − ... − pₙ`; 2) поиск всех корней `P(λ)=0` (методами из лаб. 4 — Ньютона и т.д.); 3) определение собственных векторов.

- **Метод Данилевского.** Приводят `A` подобными преобразованиями `Mₖ` к форме Фробениуса `P` (формула 47), верхняя строка которой — коэффициенты характеристического многочлена. `P = M_{n−1}⁻¹...M₁⁻¹·A·M₁...M_{n−1}`.
- **Метод Крылова.** Основан на тождестве Гамильтона–Кэли (матрица — корень своего характеристического многочлена): `Aⁿ − p₁A^{n−1} − ... − pₙE = 0`. Выбирают `x⁰`, строят `x^{i+1}=Ax^i`, получают систему `Cp = xⁿ` для коэффициентов `pᵢ` и решают её.

---

## Билет 11. Вычисление собственных векторов

После нахождения собственных значений `λᵢ` определяют соответствующие векторы:

- **Степенной метод / метод скалярных произведений.** Собственный вектор для найденного `λ` — нормированный `y = x^r/||x^r||` (последнее приближение, нормированное по максимальному по модулю элементу).

- **Метод Данилевского.** Для каждого `λᵢ` собственный вектор формы Фробениуса `y = col(λ^{n−1}, λ^{n−2}, ..., λ, 1)`; собственный вектор исходной матрицы `x = M₁M₂...M_{n−1}·y` (произведение матриц преобразования подобия).

- **Метод Крылова.** Для каждого `λᵢ` вычисляют коэффициенты `βᵢⱼ` по рекуррентной формуле (53): `βᵢ₁=1`, `βᵢⱼ = βᵢ,_{j−1}·λᵢ − p_{j−1}`, `j=2,...,n`. Тогда собственный вектор (формула 54):

```
yᵢ = βᵢ₁·x^{n−1} + βᵢ₂·x^{n−2} + ... + βᵢₙ·x⁰
```

Затем выполняют нормировку и проверку `Ax − λx ≈ 0`.

---

## Билет 12. Задача аппроксимации. Полиномиальная интерполяция. Многочлен Лагранжа

**Задача аппроксимации** — приближение функции `y(x)` (заданной таблично или аналитически) более простой функцией, обычно многочленом.

**Интерполяция.** На `[a,b]` задана сетка из `n+1` узлов `x₀<x₁<...<xₙ`, в узлах известны `yᵢ=y(xᵢ)`. Построить многочлен `L(x)` степени `≤n` такой, что `L(xᵢ)=yᵢ`, `i=0,...,n` — интерполяционный многочлен. Погрешность интерполяции `r(x)=y(x)−L(x)`; оценка (формула 5):

```
|r(x)| ≤ M/(n+1)! · |ω(x)|,   M = max|y^{(n+1)}(ξ)|,   ω(x)=(x−x₀)(x−x₁)...(x−xₙ)
```

**Многочлен Лагранжа:**

```
L(x) = Σ_{i=0}ⁿ yᵢ·lᵢ(x)

lᵢ(x) = [(x−x₀)...(x−x_{i−1})(x−x_{i+1})...(x−xₙ)] / [(xᵢ−x₀)...(xᵢ−x_{i−1})(xᵢ−x_{i+1})...(xᵢ−xₙ)]
```

Базисные многочлены: `lᵢ(xᵢ)=1`, `lᵢ(xⱼ)=0` при `j≠i`. Применим для любых, в том числе неравноотстоящих, узлов.

---

## Билет 13. Интерполяционная формула Ньютона для равномерной сетки

Сетка равномерная: `xᵢ = x₀ + ih`, `i=0,...,n`, `h=const` (шаг), узлы равноотстоящие.

**Конечные разности:**

```
Δy₀ = y₁ − y₀            (1-го порядка)
Δ²y₀ = Δy₁ − Δy₀         (2-го порядка)
...
Δᵏy₀ = Δ^{k−1}y₁ − Δ^{k−1}y₀   (k-го порядка)
```

Удобно вычислять в таблице конечных разностей (каждое значение — разность двух соседних предыдущего столбца).

**Многочлен Ньютона** (формула 7):

```
N(x) = y₀ + (Δy₀/h)(x−x₀) + (Δ²y₀/(h²·2!))(x−x₀)(x−x₁) + ...
           + (Δⁿy₀/(hⁿ·n!))(x−x₀)(x−x₁)...(x−x_{n−1})
```

> Для произвольных (неравноотстоящих) узлов используют **разделённые разности** и формулу Ньютона (6): `N(x)=y₀ + p(x₀)(x−x₀) + p(x₀,x₁)(x−x₀)(x−x₁) + ...`. Преимущество формы Ньютона: при добавлении нового узла не нужно пересчитывать всё (в отличие от Лагранжа).

---

## Билет 14. Параболические и кубические сплайны

**Сплайн** `k`-го порядка `s(x)` — функция, на каждом отрезке `[xᵢ,x_{i+1}]` являющаяся многочленом степени `≤k`, причём `s` и все её производные до `(k−1)`-го порядка непрерывны на `[x₀,xₙ]`, и `s(xᵢ)=yᵢ`. Сплайны применяют вместо интерполяционного многочлена высокой степени, который при больших `n` сильно осциллирует.

**Параболический сплайн** (лаб. 2): на каждом `[xᵢ,x_{i+1}]`

```
s(x) = aᵢ(x−xᵢ)² + bᵢ(x−xᵢ) + cᵢ
```

Из условий: `cᵢ = yᵢ` (формула 8); непрерывность `s` и `s′` дают (9–10): `aᵢhᵢ² + bᵢhᵢ = y_{i+1}−yᵢ`, `2aᵢhᵢ + bᵢ = b_{i+1}`. Число неизвестных на единицу больше числа уравнений → вводят доп. условие, например `s′(x₀)=0 ⇒ b₀=0`. Рекуррентные формулы (11–12):

```
b₀ = 0,   b_{i+1} = 2(y_{i+1}−yᵢ)/hᵢ − bᵢ
aᵢ = (b_{i+1} − bᵢ)/(2hᵢ)
```

**Кубический сплайн:** на каждом отрезке многочлен 3-й степени

```
s(x) = aᵢ + bᵢ(x−xᵢ) + cᵢ(x−xᵢ)² + dᵢ(x−xᵢ)³
```

Непрерывны `s`, `s′`, `s″`. Условия интерполяции и сшивки приводят к системе с **трёхдиагональной матрицей** относительно коэффициентов `cᵢ` (моментов), которую удобно решать **методом прогонки**. Требуется два дополнительных граничных условия (например, естественный сплайн: `s″(x₀)=s″(xₙ)=0`). Кубический сплайн даёт гладкое приближение с непрерывной кривизной.

> В методичке детально дан параболический сплайн; вывод формул для коэффициентов кубического сплайна и подсчёт числа дополнительных условий — в вопросах для самостоятельной работы (лаб. 2, вопрос 8).

---

## Билет 15. Формулы численного дифференцирования. Метод Эйлера

**Численное дифференцирование** — приближённое вычисление производных через значения функции в узлах сетки (через конечные/разделённые разности). На равномерной сетке (лаб. 8):

```
y′ᵢ ≈ (y_{i+1} − y_{i−1})/(2h) + O(h²)        — центральная разность
y″ᵢ ≈ (y_{i+1} − 2yᵢ + y_{i−1})/h² + O(h²)
y′₀ ≈ (y₁ − y₀)/h + O(h),   y′ₙ ≈ (yₙ − y_{n−1})/h + O(h)   — односторонние
```

Численное дифференцирование — **некорректная (неустойчивая)** задача: при `h→0` погрешность округления растёт.

**Задача Коши.** `y′ = f(x,y)`, `y(x₀)=y₀`, найти `y(x)` на `[x₀,X]`. Строим сетку `xᵢ = x₀ + ih`.

**Метод Эйлера.** Приближённые значения `uᵢ ≈ y(xᵢ)`:

```
u₀ = y₀,   u_{i+1} = uᵢ + h·f(xᵢ, uᵢ),   i = 0,...,n−1
```

Геометрически — движение по касательной. **Первый порядок точности:** `|y(xᵢ) − uᵢ| = O(h)`. Оценка погрешности (при условии Липшица): `|y(xᵢ) − uᵢ| ≤ hM/(2N)·((1+hN)ⁱ − 1)`.

**Модифицированный метод Эйлера** (2-й порядок): `u_{i+1/2}=uᵢ+(h/2)f(xᵢ,uᵢ)`; `u_{i+1}=uᵢ+h·f(x_{i+1/2}, u_{i+1/2})`. Тогда `|y−u|=O(h²)`.

---

## Билет 16. Квадратурные формулы Ньютона–Котеса

Для приближённого вычисления `I = ∫_a^b f(x)dx` используют квадратурную формулу `Iₙ = Σ_{i=0}ⁿ cᵢf(xᵢ)`, где `cᵢ` — коэффициенты, `xᵢ` — узлы (`a≤x₁<...<xₙ≤b`). Погрешность `Rₙ = I − Iₙ`.

**Формулы Ньютона–Котеса** — квадратурные формулы интерполяционного типа с равноотстоящими узлами (получаются интегрированием интерполяционного многочлена по равномерной сетке). Простые формулы:

- **Прямоугольников:** `I₁ = (b−a)f(ξ)`. `ξ=a` — левых, `ξ=b` — правых, `ξ=(a+b)/2` — средних. Погрешность средних: `|R₁| ≤ (b−a)³/24 · max|f″|`.
- **Трапеций:** `I₂ = (b−a)/2·(f(a)+f(b))`. `|R₂| ≤ (b−a)³/12 · max|f″|`.
- **Симпсона:** `I₃ = (b−a)/6·(f(a)+4f((a+b)/2)+f(b))`. `|R₃| ≤ (b−a)⁵/1536 · max|f⁽⁴⁾|`.

**Составные формулы** (на сетке `h=(b−a)/m`, `xᵢ=a+ih`) — сумма простых по отрезкам:

```
левых прямоугольников:  Iₘ = Σ_{i=0}^{m−1} h·f(xᵢ)
трапеций:               Iₘ = (f(x₀)+f(xₘ))/2·h + Σ_{i=1}^{m−1} h·f(xᵢ)
Симпсона:               Iₘ = (f(x₀)+f(xₘ))/6·h + Σ (h/3)f(xᵢ) + Σ (2h/3)f(xᵢ+h/2)
```

Шаг `h` подбирают **правилом Рунге**: `εₘ=|I_{2m}−Iₘ|/(2ˢ−1)` (`s=1` — прямоугольники, `s=2` — трапеции, `s=4` — Симпсон), удваивая число узлов до достижения точности ε.

> Формулы **Гауссова типа** — другой класс (неравноотстоящие узлы), точны для многочленов степени `2n−1`.

---

## Билет 17. Разностная схема для уравнения теплопроводности. Явная, неявная схемы, схема Кранка–Николсона

**Уравнение теплопроводности:**

```
∂u/∂t = ∂²u/∂x² + f(x,t),   u(x,0)=φ(x),   u(0,t)=φ₁(t),   u(l,t)=φ₂(t)
```

Сетка `xⱼ = x₀ + jh`, `tₖ = kτ`. Аппроксимации (лаб. 10):

```
∂u/∂t ≈ (u_{j,k+1} − u_{j,k})/τ + O(τ)
∂²u/∂x² ≈ (u_{j+1,k} − 2u_{j,k} + u_{j−1,k})/h² + O(h²)
```

**Явная схема** (явный четырёхточечный шаблон) — пространственная производная берётся на `k`-м слое:

```
(v_{j,k+1} − v_{j,k})/τ = (v_{j+1,k} − 2v_{j,k} + v_{j−1,k})/h² + f_{j,k}
```

Значения `(k+1)`-го слоя вычисляются **явно**:

```
v_{j,k+1} = v_{j,k} + (τ/h²)(v_{j+1,k} − 2v_{j,k} + v_{j−1,k}) + τ·f_{j,k}
```

*Достоинство:* простота, послойный явный счёт. *Недостаток:* **условно устойчива** — устойчива при `τ/h² ≤ 1/2`.

**Неявная схема** (неявный четырёхточечный шаблон) — пространственная производная на `(k+1)`-м слое:

```
(v_{j,k+1} − v_{j,k})/τ = (v_{j+1,k+1} − 2v_{j,k+1} + v_{j−1,k+1})/h² + f_{j,k}
```

На каждом слое — система с **трёхдиагональной матрицей** (формула 94), решается **методом прогонки**. **Абсолютно устойчива** (при любых `h,τ`). *Недостаток:* на каждом слое решать систему.

**Схема Кранка–Николсона** (шеститочечная, симметричная) — пространственная производная берётся как полусумма явной и неявной аппроксимаций (на слоях `k` и `k+1`):

```
(v_{j,k+1} − v_{j,k})/τ = ½·[ (v_{j+1,k+1} − 2v_{j,k+1} + v_{j−1,k+1})/h²
                            + (v_{j+1,k}  − 2v_{j,k}  + v_{j−1,k})/h² ] + f_{j,k+½}
```

Имеет **второй порядок аппроксимации** по `τ` и по `h`: `O(τ² + h²)`. **Абсолютно устойчива**. На каждом слое — система с трёхдиагональной матрицей (метод прогонки). Сочетает точность и устойчивость — компромисс между явной (простой, но условно устойчивой) и неявной схемами.

> Устойчивость исследуют методом разделения переменных `v_{jk}=λᵏe^{ijφ}` + критерий Гурвица (`|a|≤1+b`, `|b|≤1`).
